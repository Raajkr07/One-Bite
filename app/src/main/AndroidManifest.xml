<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.onebite">

    <!-- ────── Permissions ────── -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneBite"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">

        <!-- ────── Launch / splash ────── -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.OneBite.NoActionBar"
            tools:replace="android:theme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- ────── Auth flow ────── -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.OneBite.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            tools:replace="android:theme"/>

        <activity
            android:name=".RegistrationActivity"
            android:exported="false"
            android:theme="@style/Theme.OneBite.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            tools:replace="android:theme"/>

        <!-- ────── Main (menu) screen ────── -->
        <activity
            android:name=".PizzaMenuActivity"
            android:exported="false"
            android:theme="@style/Theme.OneBite.NoActionBar"
            android:parentActivityName=".LoginActivity"
            tools:replace="android:theme"/>

        <!-- ────── Cart / checkout flow ────── -->
        <activity
            android:name=".cart.CartActivity"
            android:exported="false"
            android:theme="@style/Theme.OneBite.NoActionBar"
            android:parentActivityName=".PizzaMenuActivity"
            tools:replace="android:theme"/>

        <activity
            android:name=".bill.BillGeneratorActivity"
            android:exported="false"
            android:theme="@style/Theme.OneBite.NoActionBar"
            android:parentActivityName=".cart.CartActivity"
            tools:replace="android:theme"/>
    </application>

</manifest>
