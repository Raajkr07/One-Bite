<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Custom TextInputLayout style -->
    <style name="CustomTextInputLayout" parent="Widget.Material3.TextInputLayout.FilledBox">
        <item name="boxBackgroundColor">@color/input_background</item>
        <item name="boxStrokeColor">@color/input_stroke</item>
        <item name="boxStrokeWidth">1dp</item>
        <item name="boxCornerRadiusTopStart">12dp</item>
        <item name="boxCornerRadiusTopEnd">12dp</item>
        <item name="boxCornerRadiusBottomStart">12dp</item>
        <item name="boxCornerRadiusBottomEnd">12dp</item>
        <item name="hintTextColor">@color/white_50</item>
        <item name="android:textColorHint">@color/white_50</item>
    </style>

    <!-- Custom button style -->
    <style name="CustomLoginButton" parent="Widget.Material3.Button">
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">28dp</item>
        <item name="android:fontFamily">@font/poppins_semibold</item>
        <item name="rippleColor">@color/white_20</item>
    </style>

    <!-- ShapeAppearanceOverlay for 12dp corner radius -->
    <style name="ShapeAppearanceOverlay.App.CornerSize12dp" parent="">
        <item name="cornerSize">12dp</item>
    </style>

    <!-- ShapeAppearanceOverlay for 16dp corner radius (if needed) -->
    <style name="ShapeAppearanceOverlay.App.CornerSize16dp" parent="">
        <item name="cornerSize">16dp</item>
    </style>

    <!-- Social button style -->
    <style name="SocialLoginButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">24dp</item>
        <item name="android:fontFamily">@font/poppins_regular</item>
        <item name="strokeColor">@color/social_button_stroke</item>
        <item name="strokeWidth">1dp</item>
        <item name="rippleColor">@color/white_20</item>
    </style>

    <!-- New app theme with NoActionBar -->
    <style name="AppTheme.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your NoActionBar theme here -->
        <item name="colorPrimary">@color/your_primary_color</item>
        <item name="colorPrimaryVariant">@color/your_primary_variant</item>
        <item name="colorOnPrimary">@color/your_on_primary</item>
        <item name="colorSecondary">@color/your_secondary_color</item>
        <item name="colorOnSecondary">@color/your_on_secondary</item>
    </style>

    <style name="ToolbarTitleTextStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="fontFamily">@font/yeon_sung</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.CornerSize20dp" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>


</resources>